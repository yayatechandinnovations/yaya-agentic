/**
 * Shared domain types: typed IDs, principal, session, turn, intent frame, permission requirement.
 * <p>
 * This package has no dependencies on any other yaya-agentic package. Other modules
 * (auth, tool, knowledge, recorder, engine) depend on it; it depends on none of them.
 * <p>
 * See {@code docs/design/yaya-agentic-design.md} §5.7 for session/turn semantics.
 */
package com.yayatechandinnovations.yayaagentic.core;
